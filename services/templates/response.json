{
  "status": "{{ status }}",
  "prediction": {
    "star_rating": "{{ rating }}",
    "raw": {{ prediction_raw }},
    "truth": "{{ truth }}"
  },
  "input": {
    "raw": "{{ review_raw }}",
    "truth": "{{ truth }}",
    "preprocessed": "{{ review_preprocessed }}",
    "encoded": {{ review_encoded }}
  }
}